"""
ImageFlashViewer package.

Developer Notes: This package contains the GUI (under ui/), data access
layer (data/), background services (services/), state container (state/),
and configuration (config.py). Import paths are organized for clarity
between layers; UI should use services/repo via the main window/state.
"""
